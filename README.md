# ميزانيتي - Mizaneyati

تطبيق إدارة مالية شامل مبني بـ Flutter يساعدك على تتبع نفقاتك وإدارة ميزانيتك بكفاءة.

## 📋 نظرة عامة على المشروع

**ميزانيتي** هو تطبيق جوال متقدم لإدارة الأموال الشخصية يوفر:

- ✅ إدارة الحسابات المتعددة (نقدي، بنكي، بطاقات ائتمان)
- ✅ تتبع المعاملات (مصروفات ودخل)
- ✅ تصنيف المعاملات بفئات مخصصة
- ✅ إدارة الميزانيات والتحكم في النفقات
- ✅ إحصائيات وتقارير مفصلة
- ✅ دعم العملات المتعددة
- ✅ إرفاق الإيصالات والفواتير
- ✅ نسخ احتياطي واستعادة البيانات

---

## 🏗️ معمارية المشروع

المشروع يتبع **Clean Architecture** مع **MVVM** pattern:

```
lib/
├── main.dart                          # نقطة الدخول
├── core/                              # الطبقة الأساسية
│   ├── constants/                     # الثوابت والإعدادات
│   ├── errors/                        # معالجة الأخطاء
│   ├── logging/                       # نظام التسجيل
│   ├── utils/                         # أدوات مساعدة
│   └── validation/                    # التحقق من البيانات
├── domain/                            # طبقة النطاق (Business Logic)
│   ├── entities/                      # كائنات النطاق
│   ├── models/                        # نماذج البيانات
│   ├── repositories/                  # واجهات المستودعات
│   ├── usecases/                      # حالات الاستخدام
│   ├── value_objects/                 # كائنات القيمة
│   ├── logic/                         # منطق الأعمال
│   └── validation/                    # قواعد التحقق
├── data/                              # طبقة البيانات
│   ├── local/                         # قاعدة البيانات المحلية
│   │   ├── db/                        # Drift Database
│   │   ├── daos/                      # Data Access Objects
│   │   └── mappers/                   # تحويل البيانات
│   └── repositories/                  # تنفيذ المستودعات
├── application/                       # طبقة التطبيق
│   ├── providers/                     # Riverpod Providers
│   └── viewmodels/                    # نماذج العرض
└── presentation/                      # طبقة العرض
    ├── screens/                       # الشاشات
    │   ├── dashboard/                 # لوحة التحكم
    │   ├── accounts/                  # إدارة الحسابات
    │   ├── transactions_list/         # قائمة المعاملات
    │   ├── add_transaction/           # إضافة معاملة
    │   ├── categories/                # إدارة الفئات
    │   ├── statistics/                # الإحصائيات
    │   └── settings/                  # الإعدادات
    ├── widgets/                       # مكونات مشتركة
    ├── styles/                        # الأنماط والثيمات
    └── screens/*/widgets/             # مكونات خاصة بالشاشة
```

---

## 🔧 المتطلبات والتبعيات

### متطلبات النظام
- **Flutter**: ^3.10.0
- **Dart**: ^3.10.0
- **Android**: SDK 21+
- **iOS**: 11.0+

### التبعيات الرئيسية

#### إدارة الحالة
- `flutter_riverpod: ^2.5.1` - إدارة الحالة والـ Dependency Injection

#### قاعدة البيانات
- `drift: ^2.18.0` - ORM قوي وآمن من حيث النوع
- `sqlite3_flutter_libs: ^0.5.22` - مكتبة SQLite
- `path_provider: ^2.1.4` - الوصول إلى مسارات النظام

#### الواجهة الرسومية
- `flutter_svg: ^2.0.10+1` - دعم ملفات SVG
- `google_fonts: ^6.2.1` - الخطوط من Google
- `animations: ^2.0.11` - الرسوم المتحركة
- `fl_chart: ^0.69.0` - الرسوم البيانية والإحصائيات

#### الوظائف الإضافية
- `image_picker: ^1.1.2` - التقاط الصور
- `file_picker: ^8.0.0` - اختيار الملفات
- `share_plus: ^10.0.2` - مشاركة البيانات
- `uuid: ^4.4.0` - توليد معرفات فريدة
- `intl: ^0.20.2` - دعم التدويل والتاريخ
- `logger: ^2.0.2+1` - نظام التسجيل

#### أدوات التطوير
- `build_runner: ^2.4.8` - توليد الكود
- `drift_dev: ^2.18.0` - توليد كود Drift
- `flutter_lints: ^6.0.0` - فحص الكود

---

## 📦 الكيانات الرئيسية

### 1. **الحسابات (Accounts)**
```dart
AccountEntity {
  id: String                    // معرف فريد
  name: AccountName            // اسم الحساب
  balance: Money               // الرصيد الحالي
  type: AccountType            // نوع الحساب (نقدي، بنكي، بطاقة)
  color: ColorValue            // اللون المخصص
  icon: IconValue?             // الأيقونة
  isActive: bool               // هل الحساب نشط
  createdAt: DateTime          // تاريخ الإنشاء
}
```

### 2. **المعاملات (Transactions)**
```dart
TransactionEntity {
  id: String                   // معرف فريد
  amount: Money                // المبلغ
  type: TransactionType        // نوع المعاملة (دخل/مصروف)
  categoryId: String           // معرف الفئة
  accountId: String            // معرف الحساب
  date: DateValue              // تاريخ المعاملة
  note: NoteValue              // ملاحظات
  receiptPath: String?         // مسار الإيصال
  createdAt: DateTime          // تاريخ الإنشاء
}
```

### 3. **الفئات (Categories)**
```dart
CategoryEntity {
  id: String                   // معرف فريد
  name: CategoryName           // اسم الفئة
  type: CategoryType           // نوع الفئة (دخل/مصروف)
  color: ColorValue            // اللون
  icon: IconValue              // الأيقونة
  isActive: bool               // هل الفئة نشطة
  createdAt: DateTime          // تاريخ الإنشاء
}
```

### 4. **الميزانيات (Budgets)**
```dart
BudgetEntity {
  id: String                   // معرف فريد
  categoryId: String           // معرف الفئة
  limitAmount: Money           // الحد الأقصى
  startDate: DateValue         // تاريخ البداية
  endDate: DateValue           // تاريخ النهاية
  isActive: bool               // هل الميزانية نشطة
}
```

---

## 🚀 البدء السريع

### 1. التثبيت والإعداد

```bash
# استنساخ المستودع
git clone <repository-url>
cd mizaneyati

# تثبيت التبعيات
flutter pub get

# توليد الكود (Drift + Riverpod)
flutter pub run build_runner build --delete-conflicting-outputs
```

### 2. تشغيل التطبيق

```bash
# تشغيل على الجهاز الافتراضي
flutter run

# تشغيل مع تسجيل مفصل
flutter run -v

# بناء APK للإنتاج
flutter build apk --release

# بناء iOS
flutter build ios --release
```

### 3. الاختبار

```bash
# تشغيل جميع الاختبارات
flutter test

# تشغيل اختبار محدد
flutter test test/domain/value_objects_test.dart

# تشغيل مع تغطية الكود
flutter test --coverage
```

---

## 📚 الميزات الرئيسية

### 1. **إدارة الحسابات**
- إنشاء وتحديث وحذف الحسابات
- دعم أنواع مختلفة من الحسابات
- تخصيص الألوان والأيقونات
- تتبع الرصيد الحالي

### 2. **تتبع المعاملات**
- تسجيل المصروفات والدخل
- ربط المعاملات بالفئات والحسابات
- إضافة ملاحظات وإيصالات
- تحديث وحذف المعاملات

### 3. **إدارة الفئات**
- فئات افتراضية للمصروفات والدخل
- إمكانية إنشاء فئات مخصصة
- تخصيص الألوان والأيقونات

### 4. **الميزانيات والتنبيهات**
- تعيين حدود للنفقات
- تتبع الإنفاق مقابل الميزانية
- تنبيهات عند الاقتراب من الحد
- تنبيهات عند تجاوز الميزانية

### 5. **الإحصائيات والتقارير**
- رسوم بيانية للمصروفات والدخل
- تحليل الإنفاق حسب الفئة
- مقارنة الفترات الزمنية
- تقارير قابلة للتصدير

### 6. **النسخ الاحتياطي**
- نسخ احتياطي تلقائي للبيانات
- استعادة من نسخة احتياطية
- تصدير واستيراد البيانات

---

## 🔐 معالجة الأخطاء والتحقق

### نظام النتائج (Result Pattern)

```dart
// النتيجة يمكن أن تكون نجاح أو فشل
Result<T> = Success<T> | Fail<T>

// الاستخدام
final result = await repository.createAccount(name: 'حسابي');
if (result is Success) {
  final account = result.value;
  // معالجة النجاح
} else if (result is Fail) {
  final failure = result.failure;
  // معالجة الفشل
}
```

### التحقق من البيانات

```dart
import 'package:mizaneyati/core/validation/validators.dart';

// التحقق من المبلغ
final validation = Validators.validateAmount(amount);
if (validation is Fail) {
  return Fail((validation as Fail).failure);
}

// التحقق من الحساب النشط
final activeCheck = Validators.validateAccountActive(account.isActive);
```

### الأخطاء المدعومة

- `ValidationFailure` - فشل التحقق من البيانات
- `DatabaseFailure` - أخطاء قاعدة البيانات
- `RepositoryFailure` - أخطاء المستودع
- `UnknownFailure` - أخطاء غير معروفة

---

## 📊 نظام التسجيل (Logging)

```dart
import 'package:mizaneyati/core/logging/db_logger.dart';

// تسجيل استعلام
final sw = Stopwatch()..start();
try {
  final result = await database.query(...);
  sw.stop();
  DbLogger().logQuery('SELECT ...', duration: sw.elapsed);
} catch (e, st) {
  DbLogger().logError('Query failed', e, st);
}

// تسجيل تحذير
DbLogger().logWarning('Slow query: ${sw.elapsedMilliseconds}ms');
```

---

## ⚙️ الثوابت والإعدادات

جميع الثوابت موجودة في `lib/core/constants/app_constants.dart`:

```dart
// العملة الافتراضية
AppConstants.defaultCurrency        // 'SYP'
AppConstants.defaultCurrencySymbol  // 'ل.س'

// تنسيقات التاريخ
AppConstants.displayDateFormat      // 'd MMMM yyyy'
AppConstants.shortDateFormat        // 'dd/MM/yyyy'

// الحدود المالية
AppConstants.minAmount              // 0.01
AppConstants.maxAmount              // 100,000,000,000

// الألوان والأيقونات الافتراضية
AppConstants.defaultColors          // قائمة الألوان
AppConstants.defaultIcons           // قائمة الأيقونات

// الفئات الافتراضية
AppConstants.defaultExpenseCategories
AppConstants.defaultIncomeCategories

// الحسابات الافتراضية
AppConstants.defaultAccounts
```

---

## 🧪 الاختبار

المشروع يتضمن اختبارات شاملة:

```bash
# اختبارات Value Objects
test/domain/value_objects_test.dart

# اختبارات Widget
test/widget_test.dart
```

---

## 📁 ملفات مهمة

- `pubspec.yaml` - التبعيات والإعدادات
- `analysis_options.yaml` - قواعد تحليل الكود
- `lib/core/README_VALIDATION_LOGGING.md` - دليل التحقق والتسجيل
- `lib/core/constants/README_CONSTANTS.md` - دليل الثوابت
- `lib/domain/usecases/transaction_usecase_example.dart` - أمثلة UseCases
- `lib/data/local/daos/accounts_dao_example.dart` - أمثلة DAOs

---

## 🐛 معالجة المشاكل الشائعة

### مشكلة: فشل توليد الكود
```bash
# الحل
flutter clean
flutter pub get
flutter pub run build_runner build --delete-conflicting-outputs
```

### مشكلة: أخطاء قاعدة البيانات
```bash
# حذف قاعدة البيانات القديمة وإعادة الإنشاء
flutter clean
flutter pub get
flutter run
```

### مشكلة: تضارب الإصدارات
```bash
# تحديث التبعيات
flutter pub upgrade
flutter pub get
```

---

## 📝 معايير الكود

- اتبع **Dart Style Guide**
- استخدم **meaningful names** للمتغيرات والدوال
- أضف **documentation comments** للفئات والدوال العامة
- استخدم **const constructors** حيثما أمكن
- اتبع **Clean Architecture** principles
- استخدم **Result Pattern** للتعامل مع الأخطاء

---

## 🤝 المساهمة

1. إنشاء فرع جديد (`git checkout -b feature/amazing-feature`)
2. Commit التغييرات (`git commit -m 'Add amazing feature'`)
3. Push إلى الفرع (`git push origin feature/amazing-feature`)
4. فتح Pull Request

---

## 📄 الترخيص

هذا المشروع مرخص تحت MIT License - انظر ملف LICENSE للتفاصيل.

---

## 📞 التواصل والدعم

للأسئلة والدعم، يرجى فتح issue في المستودع.

---

## ✅ حالة المشروع

- ✅ معمارية Clean Architecture
- ✅ إدارة الحالة مع Riverpod
- ✅ قاعدة بيانات Drift
- ✅ نظام التحقق والتسجيل
- ✅ اختبارات Value Objects
- ✅ ثوابت وإعدادات شاملة
- ⏳ تطوير الشاشات والواجهات
- ⏳ تطوير الإحصائيات والتقارير
- ⏳ نظام النسخ الاحتياطي
- ⏳ تطبيق التنبيهات والإشعارات

---

تم آخر تحديث: ديسمبر 2025
